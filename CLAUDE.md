# Code style

Do not add code comments (JS/TS/TSX/CSS/SCSS `//`, `/* */`, or JSX `{/* */}`) anywhere in this repo, including explanatory comments on complex logic, unless the user explicitly asks for a comment in that specific instance. Write self-explanatory code (clear naming, small functions) instead of leaning on comments.
